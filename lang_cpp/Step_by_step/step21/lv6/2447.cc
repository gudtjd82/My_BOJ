#include <iostream>
#include <string>
#include <vector>

using namespace std;



string make_stars(int n)
{
    for(int i = 0; i < n; i++)
    {
        
    }
}

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int n;

    cin >> n;
    
    return 0;
}